from typing import Dict

def build_full_report(
    face_info: Dict,
    emotion_profile: Dict,
    stress_info: Dict,
    personality: Dict,
    professional: Dict,
):
    age = face_info.get("age", "—")
    gender = face_info.get("gender", "—")
    dom_em = emotion_profile.get("dominant_emotion", "—")
    valence = emotion_profile.get("valence", "—")
    intensity = emotion_profile.get("emotional_intensity", "—")
    stability = emotion_profile.get("emotional_stability", "—")
    emo_tendencies = emotion_profile.get("tendencies", [])
    stress_level = stress_info.get("microstress_level", "—")
    stress_notes = stress_info.get("notes", [])
    scores = personality["big_five_scores"]
    desc = personality["big_five_descriptions"]
    radical = personality["radical"]

    def bf_line(trait_key, name_ua):
        return f"- **{name_ua}** ({scores[trait_key]}/100): {desc[trait_key]}"

    roles = professional["recommended_roles"]
    work_style = professional["work_style"]
    risks = professional["risks"]
    comm = professional["communication_style"]

    report = f"""
🧠 **Психологічний портрет за фото (евристичний аналіз)**

---

## 1. Базова інформація

- Орієнтовний вік: **{age}**
- Стать (за моделлю розпізнавання): **{gender}**
- Домінантна емоція на фото: **{dom_em}**
- Загальна емоційна валентність: **{valence}**
- Інтенсивність емоційної реакції: **{intensity}**
- Стабільність емоційного профілю: **{stability}**

"""

    if emo_tendencies:
        report += "\n### Емоційні тенденції:\n"
        for t in emo_tendencies:
            report += f"- {t}\n"

    report += f"""

---

## 2. Рівень напруги / мікростресу

- Орієнтовний рівень мікростресу: **{stress_level}**

"""
    if stress_notes:
        report += "Коментарі моделі щодо стану:\n"
        for n in stress_notes:
            report += f"- {n}\n"

    report += f"""

---

## 3. Особистісний профіль (Big Five, евристична оцінка)

{bf_line("openness", "Відкритість до досвіду")}
{bf_line("conscientiousness", "Сумлінність")}
{bf_line("extraversion", "Екстраверсія")}
{bf_line("agreeableness", "Доброжичливість")}
{bf_line("neuroticism", "Нейротизм")}

> Ці показники не є клінічним тестом, а лише наближеним портретом,
> сформованим за виразом обличчя та загальним емоційно-стресовим фоном.

---

## 4. Радикали (Пономаренко) – ймовірний домінуючий

- Ймовірно домінує: **{radical}**

Це означає, що у поведінці можуть простежуватися характерні для цього радикала риси:
- своєрідний емоційний стиль реагування;
- специфічний спосіб відстоювання своїх інтересів;
- типова стратегія поведінки у конфліктах та стресі.

*(Це евристична оцінка, не клінічна діагностика.)*

---

## 5. Поведінка у спілкуванні та стосунках

На основі емоційного профілю та особистісних рис можна припустити:

- Як людина сприймає інших: 
  - { "більш довірливо та доброзичливо" if scores["agreeableness"] >= 60 else "обережно, з аналізом мотивів" }
- Як проявляє себе:
  - { "активно, ініціативно, з помітною присутністю" if scores["extraversion"] >= 60 else "стримано, вибірково, з акцентом на безпечних контактах" }
- Емоційний фон у стосунках:
  - { "переважно теплий та підтримувальний" if scores["agreeableness"] >= 60 else "може бути контрастним – від тепла до жорсткості залежно від ситуації" }

---

## 6. Поведінка в роботі та ролі, де людина може бути ефективною

**Рекомендовані ролі (за рисами та радикалом):**
"""

    for r in roles:
        report += f"- {r}\n"

    report += "\n**Робочий стиль:**\n"
    for w in work_style:
        report += f"- {w}\n"

    report += "\n**Можливі ризики у професійній діяльності:**\n"
    for r in risks:
        report += f"- {r}\n"

    report += """

---

## 7. Стиль комунікації, який підходить цій людині

"""

    for c in comm:
        report += f"- {c}\n"

    report += """

---

## 8. Загальні рекомендації

- Важливо враховувати емоційний фон та рівень напруги — у стані перевантаження
  характерні риси можуть проявлятися більш різко.
- Для ефективної взаємодії корисно поєднувати повагу до меж людини та
  достатню ясність у домовленостях.
- Портрет, побудований за одним фото, відображає **моментний зріз стану**, 
  а не повну глибину особистості. Для більш точного аналізу потрібні
  додаткові дані (поведінка в динаміці, відповіді на опитувальники, інтервʼю).

---

> ⚠️ **Важливий дисклеймер:** цей аналіз не є психіатричним або
> клінічним висновком. Це евристичний психологічний портрет, побудований
> алгоритмом на основі зображення обличчя та типових кореляцій між
> емоційним виразом, стресом та особистісними характеристиками.
"""

    return report